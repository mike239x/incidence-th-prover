def prove(config, select):
    '''Generate a proof that selected points are lying on the same line.

    Keyword arguments:
    config -- configuration of points and lines in RP2.
    select -- selectd points from the configuration.
    '''
    pass
